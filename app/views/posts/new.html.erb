<h1>Sukurti naują pranešimą</h1>
<%= form_for :post, url: posts_path do |f| %>
    <%if @post.errors.any?%><%# Jei bus gauti errorai, tada suveiks cikslas ir atvaizduosime kokia buvo klaida%>
    <% @post.errors.full_messages.each do |msg|%>
        <div class="alert alert-danger">
            <%=msg%>
        </div>
        <%end%>
    <%end%>
    <p>
    <%= f.label :title %>
    <br>
    <%= f.text_field :title, :class => 'form-control' %>
    </p>

    <p>
    <%= f.label :body %>
    <br>
    <%= f.text_area :body, :class => 'form-control' %>
    </p>

    <p>
    <%= f.submit :class => 'btn btn-primary'%>
    </p>
<%end%>
<%# Naudoju form_for helper yra skirtas post, kur f
skirtas sukurti label ir field ir button su tipu submit.
Mano pavadinimai label yra priskirti laukeliui i kuri vesiu informacija.
Ir mes galesime pateikti duomenis musu create metodui%>