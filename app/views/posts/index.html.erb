<h1 style="text-align: center">Pranešimai</h1>
<% @posts.each do |post|%>
<div class="well">
    <h3><%= post.title%></h3>
    <p><%= post.body%></p>
    <%if session[:user]!=nil then%>
        <%= link_to "Read More", post_path(post), :class => 'btn btn-primary'%>
    <%end%>
</div>
<%end%>