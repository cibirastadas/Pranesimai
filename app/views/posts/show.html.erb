<h2>Pranešimas <%= @post.id%></h2>
<h2><%= @post.title %></h3>
<p><%= @post.body %></p>
<%if session[:user]['name']=="Admin" then%>
    <%= link_to "Edit", edit_post_path(@post), :class => 'btn btn-primary'%>
    <%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: 'Ar jus tikrai norite panaikinti įrašą'}, :class => 'btn btn-danger'%>
<%end%>
<hr>
<%= render 'comments/comments'%>
<%= render 'comments/form'%>
<%# Gaunu post duomenis uzpildyto %>